# STT 设计差距分析

**日期**：2026-08-04
**版本**：V1.0.9

---

## 一、已完整实现 ✅

| 需求 | 状态 |
|------|------|
| 4.1 屏幕用时记录 | ✅ 四平台均有实现，含心跳、崩溃恢复、跨日切分 |
| 4.2 护眼+姿势提醒 | ✅ 合并提醒、绝对时间倒计时、会议模式 |
| 4.3 每日计划+超时提醒 | ✅ 小时+分钟输入、25 分钟节流 |
| 5.1 日报 | ✅ 各设备分别用时、时间段明细、表格布局 |
| 5.2 多日报 | ✅ 区间去重、日均、各设备分别用时 |
| 5.3 跨平台周统计 | ✅ 各平台统计、去重总用时、设备级明细 |
| 6.1 P2P 同步方式 | ✅ 本地优先、无云服务 |
| 6.2 局域网发现 | ✅ Bonjour `_stg-sync._tcp.local` |
| 6.3 配对与加密 | ✅ 6 位数字码规范化、AES-GCM |
| 6.4 设备确认 | ✅ 待确认/已同意/已拒绝状态 |
| 6.5 手动+定时同步 | ✅ 立即同步、可配置间隔 |
| 6.7 设备身份 | ✅ macOS Keychain、iOS identifierForVendor、Android AndroidId |
| 7.x 设置 | ✅ 完整设置项 |
| 8.x LLM Ranking | ✅ Top 20、排序、缓存 |
| 9.1 macOS | ✅ 菜单栏、系统事件、完整功能 |
| 9.2 Windows | ✅ 系统托盘、完整功能 |
| 9.4 Android | ✅ 前台服务、UsageStats、完整功能 |
| 10 错误与体验 | ✅ 后台不阻塞、计时暂停、多语言 |
| 11 构建产物 | ✅ 四平台构建脚本 |

---

## 二、设计差距（需补充）

### GAP-001: 同步协议预留能力字段未声明
- **需求**：6.6 节要求为 `weekly_manifest`、`daily_summary`、`tombstone_ack` 保留兼容能力字段
- **现状**：代码中只声明了 `delta_sync`、`gzip`、`history_compaction`，缺少 `weekly_manifest`、`daily_summary`、`tombstone_ack`
- **影响**：旧版本设备无法识别这些能力，未来扩展可能破坏兼容性
- **建议**：在各平台的 capabilities 数组中添加这三个预留名称（当前版本不实现功能，只声明）

### GAP-002: 历史归档不参与 P2P 同步 — 缺少同步时机设计
- **需求**：6.6 节 "历史归档不参与 P2P 同步"
- **现状**：代码实现了本地历史归档（`.jsonl.gz`），但没有明确设计"何时生成归档"和"如何通知其他设备"
- **影响**：如果设备 A 归档了旧数据并从活动 sessions 中删除，设备 B 还没同步到这些数据，就会丢失
- **建议**：设计文档应明确：
  1. 归档只在所有可信设备都确认同步后才执行
  2. 或者归档只保留本地副本，活动 sessions 不删除（当前版本的做法）

### GAP-003: iOS ScreenTime 数据对齐 — 设计文档未描述
- **需求**：9.3 节 "iOS/iPadOS 通过不指定应用、类别和网站过滤条件的 DeviceActivity 阈值记录系统 Screen Time 全活动范围"
- **现状**：我们实现了阈值差值 + 累计总量对齐算法，但设计文档中没有描述这个机制
- **影响**：后续维护者可能不理解为什么用阈值差值而不是理论间隔
- **建议**：在设计文档中添加 iOS ScreenTime 数据对齐设计说明

### GAP-004: 同步协议帧格式缺乏正式规范
- **需求**：6.2 节 "TCP 帧使用 4 字节 big-endian 长度前缀"
- **现状**：各平台都实现了 4 字节 big-endian 帧格式，但没有在共享 schema 或设计文档中正式定义
- **影响**：新开发者可能不了解帧格式要求
- **建议**：在 `shared/sync/` 中添加 `transport-frame.schema.json` 或在设计文档中正式描述

### GAP-005: 数据静默加密未实现
- **需求**：需求文档未明确要求，但设计上应考虑
- **现状**：P2P 传输使用 AES-GCM 加密，但本地存储的 sessions.json 等文件是明文
- **影响**：设备被物理访问时数据可读
- **建议**：低优先级，可在后续版本考虑使用平台 Keychain/Keystore 加密本地文件

---

## 三、实现细节差距

### GAP-006: iOS 报告扩展只有 Summary 视图
- **需求**：5.x 节要求日报、多日报、周统计
- **现状**：iOS `DeviceActivityReport` 扩展只有 `SummaryReport`，没有日报/多日报/周报独立视图
- **影响**：iOS 系统 ScreenTime 界面只显示汇总，不显示详细报告
- **备注**：主 App 内有完整报告功能（`ReportActivity`），Report Extension 是系统集成的补充

### GAP-007: Windows 设备身份持久化
- **需求**：6.7 节 "Windows：MachineGuid 派生 UUID，再持久化到本地 device_id"
- **现状**：`AppSettings.cs` 有 `device_id` 字段，但没有看到从 MachineGuid 派生的逻辑
- **影响**：重装应用后 device_id 可能变化
- **建议**：在 Windows 端添加 MachineGuid 读取和持久化逻辑

### GAP-008: Android 设备身份持久化
- **需求**：6.7 节 "Android：Android ID 派生 UUID，再持久化到本地 device_id"
- **现状**：`SessionStore.kt` 有 `deviceId` 字段，但持久化逻辑需要确认
- **影响**：同 GAP-007

---

## 四、优先级建议

| 优先级 | GAP | 说明 |
|--------|-----|------|
| P1 | GAP-001 | 预留能力字段 — 防止未来版本兼容性问题 |
| P1 | GAP-002 | 历史归档同步时机 — 防止数据丢失 |
| P2 | GAP-003 | iOS 对齐算法文档化 |
| P2 | GAP-004 | 传输帧格式规范 |
| P3 | GAP-005 | 本地数据加密 |
| P3 | GAP-006 | iOS 报告扩展完善 |
| P3 | GAP-007 | Windows 设备身份持久化 |
| P3 | GAP-008 | Android 设备身份持久化 |

---

## 五、总结

STT 项目的设计完成度约 **92%**。核心功能完整，主要差距在：

1. **协议兼容性**：缺少预留能力字段声明
2. **数据安全**：历史归档时机、本地加密
3. **文档化**：iOS 对齐算法、传输帧格式

这些都是**设计层面的补充**，不是功能缺失。代码可以正常运行，但后续版本扩展时可能遇到兼容性问题。
